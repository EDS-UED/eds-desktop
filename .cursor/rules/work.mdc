---
description: EDS Desktop 工作约定（唯一真源）
alwaysApply: true
---

# EDS Desktop — 工作约定（唯一真源）

> **§0** 执行（含 §0.1 工作推进） · **§1** token · **§2** 公共 API · **§3–4** Showcase · **§5** 作用域/HMR · **§6** 滚动 · **§7** Organism · **§8** 同步 · **§9** 验证 · **§10–18** 专项 · **§19** 变更
>
> 外部引用已固定：consumer `work-cregis-desktop` 指向本文 **§7**（EgPopup）与 **§13**（顶底毛玻璃）。**改这两节编号须同步 consumer 的 `AGENTS.md` 与 `.cursor/rules/work.mdc`。**

## 0. Agent 执行范围

- 只做用户明确要求的事；**最小 diff**；禁止顺带改 layout、API、consumer、文档、测试。
- 不确定先问；「同步」→ §8；「生成数据」→ §4。
- 无 token / 无 prop 枚举 → **停改**，先问能否硬编码。

| 用户说 | ✅ | ❌ |
|--------|----|----|
| 表头溢出也用 `…` | 表头 `text-overflow: ellipsis` | 改 flex、tooltip、combo |
| 同步 | §8 全流程 | 改 consumer 业务、自动 commit |

### 0.1 Agent 工作推进（大阶段 · 强逻辑）

与用户协作时，**默认按大阶段推进并在过程中写清楚逻辑**；禁止闷头改完再打包汇报。

**粒度**：只在大阶段写；阶段内不逐文件、不逐命令啰嗦。

**每阶段格式**：`*思考：…*` → **做了什么**（1–3 句，因果清楚，必要时点出依据哪条 §）。

| 阶段 | 思考要点 | 交付 |
|------|----------|------|
| **定目标** | 这次解决什么；边界（EDS / consumer / 不改什么） | 目标一句 |
| **摸底** | 先弄清现状与约束再改 | 关键发现、风险、是否阻塞 |
| **实施** | 最小 diff；EDS 真源 → sync → consumer | 改了哪一层、为何这样改 |
| **验证** | 没过 §9 不算完成 | 跑了什么、通过/失败；**区分「本次引入」与「预先存在」** |
| **收尾** | 不重复过程 | 2–3 句：完成项 + consumer 待办（如有） |

**逻辑性要求**：先因后果；改 EDS 公共 API 必提 consumer 迁移；报失败须说明是本次还是既有问题。

consumer 项目 `.cursor/rules/work.mdc` 须与本节对齐（业务侧增补双仓 pin / i18n 等约束）。

## 1. Token 与样式

**作用域**：Showcase 全站 + Desktop 组件 → `@eds/desktop-tokens` / `@eds/desktop-components`；组件预览 subtree 加 `.desktopTokens` 锁排版基线。

1. **语义优先**：查 `packages/tokens/spec/**`、`dist/css/**`。禁止用 `--scale-*` 顶替 `--radius-*` / `--spacing-*` / `--stroke-*`。
2. **禁止私造 `--*`**、局部 alias、手拼 shadow/玻璃。阴影 token 仅 `--eds-shadow-{deep|light|molde|subtle}`；玻璃/卡片用 `.effect-*`（`container-box`、`flotation-box`、`popover-box`、`popup-box`、`molde-level`、`subtle-card`）。
3. **Motion**（base → recipe → semantic → EDS glue）：
   - **base**：primitive only（`--eds-motion-*`）。**禁止**场景名。
   - **recipe**：`--motion-recipe-*`。**仅 EDS glue** 消费。
   - **semantic**：`motion/semantic.css` 的 `.motion-page`、`.motion-flotation`、`.motion-layout`、`.motion-layout-deform`、`.motion-deform`、`.motion-ease` — 完整场景默认。**禁止** `enter-from`/`leave-to`/`host*`/方向 modifier。`.motion-none` 在 `motion/utilities.css`。
   - **EDS glue**：Vue lifecycle、叠层、方向状态；例：`DataList` 批选 `<col>` 宽度过渡、selection snapshot、overflow 测量冻结 — **非** semantic 场景类。**禁止**写入 `spec/motion/semantic.json`。详案：`layout-deform-page-switch.md`。
4. **microFloat**：`opacity`/`transform` 与 `backdrop-filter` **同在 glass**（`.effect-flotation-box` / `.effect-popup-box`）；glue `styles/overlayGlassMicroFloat.module.css`；业务 `.motion-flotation`。浮层微浮动**已默认启用**，无开关 prop。
5. **EgPopover（视觉锁死）**：默认宽 336（`popoverShape.ts` `POPOVER_PANEL_W`）；fixed 预置 256 引导 / 296 二次确认 / 336 基础 / 460 复杂。只说改 shadow/颜色时 **禁止**动 layout。用 `.effect-popover-box`（非 flotation）；阴影仅 SVG `.outline` filter。禁止为修闪烁去掉 opacity 动画或 hover `v-if` 销毁 Teleport。
6. **Typography**：整组角色 → `.typography-*`（`@eds/desktop-tokens/text/styles`）；单轴覆盖用 `--eds-*-size/weight/line-height`。
7. **其它**：顶边距用 `height: var(--padding-top-{xs|sm|md|lg})`（禁 `padding-top: %`）；数字 `formatGroupedNumber()` + `tabular-nums`；Bar 11px → `.eds-bar-text-subpixel`（`spec/typography/bar-subpixel.json`）或 NavBar sizer/paint（§7）。

| 说法 | 用 |
|------|-----|
| 圆角 12 / 间距 12 / 1px 边 | `--radius-md` / `--spacing-3` / `--stroke-sm` |
| 13px 常规/中黑 | `--eds-body-small-*` / `--eds-body-small-strong-*` |

顶底毛玻璃 → **§13**；优先 `EgSkid` / `EgToolBar` / `Paginer`。

## 2. 公共 API 与废弃

- **不留 `@deprecated`**：`packages/components` 公共 export 一旦决定废弃就**当轮删干净** —— prop、类型、`index.ts` 导出、内部兼容分支（`resolvedXxx` 转换）一并移除，不留「先标记后清理」的中间态。
- **破坏性变更必须同轮迁移 consumer**：删/改公共 export 后按 §8 同步、按 §9 验证。EDS 侧 build 全绿**不代表** consumer 可用 —— `no exported member` 只在 consumer typecheck 暴露。
- **名字互换要先让位再占位**：如 `EgTooltip` 由「面板」改指「浮层」、面板改称 `EgTooltipPanel`，批量替换须 **旧面板名 → 新面板名** 在前、**旧浮层名 → `EgTooltip`** 在后；顺序反了会产生 `Duplicate identifier`，且可能把两个组件语义调换而不报错。改完须逐个核对内外层嵌套。
- **ripgrep 默认跳过隐藏目录**：全仓改名扫描须带 `--hidden`，否则漏掉 `.cursor/rules/**`。

## 3. Showcase 目录

顶栏：**Tokens · Animations · Components · Patterns · Workflows**（详 **§14**）。`/patterns` ≠ 组件侧栏 Scenes。

- `catalog.ts` `children[].id` = 预览页路由 slug = 预览根 `anchor-id` 后缀；新增：**先 catalog + slug，再 section**。
- 同族一条整合 import；第二 child 起加 `previewGroupLabel` 分割线。
- 本体组件 slug / 公共 `Eg*` API 非明确要求不改；**场景化**新增或对齐须 **§17**。
- **新增** Scenes 子路由 slug 一律 `{family}-scene-{scene}`（kebab-case）；禁 `scens` 等拼写错误。存量例外见 §17。

## 4. 文案与演示数据

章节中文；prop 标签「中文 + 英文 token」（`showcasePropLabels.ts`）；prop 名/Eg* 英文。

**生成数据**：仅临时 `*DemoCustomizeState` 或会话 `Object.assign`；**不写** `*CustomizeDefaults`、不 localStorage；`:customize-defaults` 始终永久 defaults。

## 5. 库作用域 · HMR

| 层 | 用 |
|----|-----|
| Showcase（全站） | `@eds/desktop-tokens` / `@eds/desktop-components` |
| 组件预览 subtree | `.desktopTokens` 包 subtree |

**禁止**：`eds-website` / `@eds/website-*` 进入本仓 showcase；`desktopTokens` 打在 Eg* 根上；Desktop CSS 用非 desktop token。

**HMR**：`packages/components/**` → alias+HMR；`tokens/spec/**` → watch rebuild+reload。未生效先查 HMR/dist/specificity，勿先说 build。

**Native reset**（`styles/global.css`）：禁 `font: inherit`（仅 `font-family: inherit`）；禁 `button:not(.eds-xxx)` 抬特异性；typography 用 `.class:global(.eds-*)`；`textarea` reset 排除 `.eds-textarea-control`。

## 6. 嵌套滚动边界

内部滚动组件（NavBar、ModuleMenu、FlotationMenu、DataList、Dialog、Detail、Skid 等）：顶/底 **不**链式滚外层。`overflow-y: auto` + `overscroll-behavior: none`；JS：`wheel`（passive:false）+ scroll 钳制 + `SCROLL_EDGE_EPSILON = 2`（各组件本地声明，值须一致）。参考 `NavBar.vue`。

## 7. Organism · Showcase 场景化

**模式**：`ComponentDocLayout` +「场景化」下拉；`customize.scenario` 不进 snippet（`omitKeys`）。

### EgPopup — 外壳 vs 内容（锁死）

外壳：全窗遮罩、空白/Esc、进出场、按 `uses` 定尺寸。**不做**标题/按钮等内容。内容：`EgDetail`/`EgDialog`/`EgVerify` 各自页配置。Popup host 挂**客户端 shell**（禁列表页 subtree）；遮罩须盖 NavBar+侧栏+主区。

| 页面 slug | 定制范围 |
|-----------|----------|
| `popup` | uses、dialog 类型、开关演示 |
| `popup-scene-{detail\|dialog\|verify}` | Popup 外壳 × 内容组合 |
| `detail` / `dialog-standard` + `dialog-scene-*` / `verify` + `verify-scene-*` | 各内容全文案与控件 |
| `popovers-popover` | EgPopover 方向矩阵 |
| `popovers-scene-*` | 各场景 `Eg*Popover`（§17） |

`EgDialog`：Symbol/Compose/Standard（场景化 `EgSymbolDialog` → `dialog-scene-symbol`、`EgBusinessDialog` → `dialog-scene-compose`）。`EgVerify` 尺寸随 type 固定不可改。

### Popovers Scene

| 侧栏 slug | scenario | EDS 组件 |
|-----------|----------|---------|
| `popovers-scene-guidance` | `guidance` | `EgGuidancePopover` |
| `popovers-scene-remark` | `remark` | `EgRemarkPopover`（详案 `remark-popover.md`） |
| `popovers-scene-gas-fee` | `gas-fee` | `EgGasFeePopover`（详案 `miner-fee-popover.md`） |
| `popovers-scene-confirm` | `confirm` | `EgConfirmPopover` |

- 引导视觉在 `EgGuidancePopover`（256px match-primary preset）；**禁止** Showcase 手搓 host CSS 覆写 `EgPopover`。
- 备注 / 二次确认：`teleport-to="body"`；placement/align 变须 `updatePosition()`。
- 矿工费：`EgGasFeePopover` 封装 `miner-fee/*` panel；DOM 标记 `data-miner-fee-popover`（EVM/BTC，保留 spacing-3）| `data-miner-fee-ton-like` / `data-miner-fee-tron`（无左右缩进）。
- 旧 slug `popovers-scens-*` 仅 router 重定向，catalog 不回退。

### Layout Deform（Popover 多页 A↔B）

**禁止** `.motion-page` / `mode="out-in"` 替代。须 `useMotionLayoutDeformPageSwitch`；shell 高度 morph；内容 **+offset→0**；layout 绑 **`activePage`**。详案：`layout-deform-page-switch.md`。

### Flotation

- Combo 须 `EgFlotation` 链路 + `flotationDocCustomize.ts`；禁手搓 `EgTooltip`（AnchoredTooltip）菜单。
- Box 页：场景 `scene*` 键独立；`eds-flotation-menu--box-doc`；scroll-fade 仅 `menuListScrollable` mask。
- `EgModuleMenuItem`：`.motion-ease.is-enter-only`；禁 NavBar `is-paint`。

### EgDetail · Apply_Item（详案 `detail-apply-item.md`）

业务 **必须** `createDetailApplyItemRow(variantId, overrides)`；挂件由 `applyItemPresets.ts` 锁死。仅可覆盖：`key|title|value|tag|valueSymbolCrypto|valueIcon|valueSymbolAvatarName`。禁手拼 `DetailItemData`、CSS 藏挂件。

多地址：`addressLayout: 'multi-collapsed'` + `addressCount` + `valueEntries`；禁手拼 `address (N)`。`toolbarDividerPinned` 控制分割线，禁 CSS 强写。

### EgDataList · 批选布局动效（EDS internal）

**公共 API**：仅 `EgDataList`。**禁止**导出独立动效 Vue 组件（如 `EgSelectionMotion`）；业务只控 `selectMode`、选中数据与批处理流程，consumer 规范见 `work-cregis-desktop` `.cursor/rules/work.mdc` §7.6。

**Motion 分层**：共享 recipe — `--motion-duration-shift`、`--motion-easing-standard`、`--motion-recipe-shift`（`<col>`）、`--motion-recipe-float`（Batch Bar）。**禁止**写入 `spec/motion/semantic.json` 或新增 `.motion-*` 场景类；列宽 morph、`<colgroup>`、responsive 端点、snapshot、settle 均属 EDS glue，留在 `data-list/` internal。

**真源**：`organisms/data-list/` 的 `DataList.vue`、`DataList.module.css`、`useResponsiveColumns.ts`；测量工具在 `src/utils/overflowMeasureDuringLayout.ts`。详案：`work-cregis-desktop/docs/eds-data-list-selection-layout-motion-handoff.md`。

**契约**：`visibleSlotIndices = idleSlotIndices`（常态列 DOM 常驻）；idle / select 两套 width endpoint，`selectMode` 一次切换目标值；`<col class="layoutColumn">` + `data-eds-data-list-layout-animating`；`selectionExitSnapshot` 服务退场画面；入场/离场同一距离、时长、easing。`--motion-duration-shift`（300ms）、`SELECT_COLUMN_ANIM_MS`、`DATA_LIST_LAYOUT_SETTLE_MS`（320ms）**三处同步**。

**禁止回退**：rAF 每帧写列宽响应式状态；动画中 `v-if` 切列集或 `visibleSlotIndices` 随 progress 变化；普通退出 remount `EgDataList`（`:key`）；为批选新建 semantic 动效类或从 `index.ts` 导出 layout controller。

**未来抽取**：仅 EDS 内出现第二个 table/grid 消费方时，才提取 `usePersistentWidthTransition`；responsive column、Batch Bar、virtual rows 仍属 DataList domain。

### 其它

NavBar preset → `presets/<domain>/`；hover 过渡 only，选中 `.motion-none`。Data List 空态 `eds-business-7`。Module Menu：Cregis/UDun 独立 preset。

## 8. 本地同步

| 用户说 | 行为 |
|--------|------|
| 「同步」 | build eds-desktop → **所有** link consumer `pnpm install` |
| 「同步到 X」 | 仅指定项目 |
| 「列出可同步项目」 | `pnpm sync:list`，不 build |

**单向**：EDS → consumer ✅；consumer `src/**` 回写 `packages/**` ❌（为适配 EDS 新 API 而改 consumer `src/**` 属正向迁移，允许）。步骤：`pnpm sync`（或 build:tokens+components）→ sync:list → 各 consumer install → **按 §9 验证** → 汇报。

扫描 `~/Projects/**/package.json` 含 `link:`/`file:` 指向 `eds-desktop/packages/*`。`work-cregis-desktop`：100% Desktop；含 list-field 辅助 ts；**不含** `listFieldCryptoSampleAddresses.ts`、showcase、website。consumer 顶底毛玻璃真源 **§13**。勿只 build 不同步；build 失败则停。

## 9. 验证与交付

改动未过下表对应行 = **未完成**，不得汇报「已完成」。

| 改动面 | 必跑 |
|--------|------|
| `packages/components/**` | `pnpm build:components`（内含 `vue-tsc --noEmit` + dts rollup） |
| `packages/tokens/**` | `pnpm build:tokens` |
| `apps/showcase/**` | `pnpm --filter @eds/desktop-showcase build` |
| 公共 export 增删改名（§2） | 上述 + `pnpm sync` + consumer `pnpm vue-tsc --noEmit` + `pnpm build` |
| consumer 侧改动 | `node scripts/verify-*.mjs` 全绿（`verify-pages-artifact` 需先产出 `dist/deploy-manifest.json`，无产物时报 ENOENT 属预期） |

- 批量改名后须**回扫残留**（带 `--hidden`），并确认无因替换产生的重复项／子串误命中（如 `includes('EgTooltip')` 会被 `EgTooltipPanel` 命中，应改词边界正则）。
- 报告须区分「本次引入」与「预先存在」的失败：拿 `git show HEAD:<file>` 复跑可判定。

## 10. Display P3

双声明：hex 仅 fallback；**canonical = `color(display-p3 …)` 行**。消费须 `var(--*)`；mix 用 `color-mix(in display-p3, …)`；禁组件内 sRGB hex 覆盖 token。

## 11. Corner Smoothing

全局 squircle **60%**（`--corner-smoothing: 0.6`）；`var(--radius-*)` 禁硬编码 px。`border-radius` 在视觉圆角元素上；禁 per-component clip-path（全局 scanner）。例外：`.effect-{container|flotation|popover|popup}-box` 四件套、opt-out `data-no-corner-smoothing`。动态 UI：`rescanCornerSmoothing(root)`（`@eds/desktop-tokens/corner-smoothing`）。

## 12. SVG 描边（EgIcon）

禁 `vector-effect: non-scaling-stroke`。`stroke-width: calc(var(--eds-icon-stroke-screen) * 32 / var(--eds-icon-display-px))`（`atoms/icons/Icon.module.css`）。详案：`icon-stroke-scaling.md`。

## 13. Top & Bottom Mask

固定高面板内纵向滚动：顶/底 sticky + `var(--effect-mask)` + `var(--eds-blur-bg)`。单滚动容器；禁 fixed 顶底栏；禁不透明 scrim。状态：`SCROLL_EDGE_EPSILON = 2`。优先 `EgSkid`/`EgToolBar`/`Paginer`；参考 `Skid.vue`、`styles/frostedPageChrome.css`。

## 14. EverGreen IA

一级：**Tokens / Animations / Components / Patterns / Workflows** → `/tokens` … `/workflows`。

Components：Atoms / Molecules / Organisms / Templates；默认本体 + `navSection: Scenes`。**Variant ≠ Scene**（定制区 vs 侧栏 Scenes 子路由）。Flotation 例外：Trigger/Box/Combo 用 `navSubgroup`。catalog 构造：`catalogHelpers.ts`（`catalogBody` / `catalogScene` / `catalogScenesSection` / `catalogFamilyBodyAndScenes` / `catalogFamilyBodyWithBusinessPresets`）、`navSection`、`buildCatalogAnchorItems`。

## 15. 组件文档扩展层

五层：**Variants / Props / Slots / Composition / Scenarios**（`extensionDelivery.ts` `ExtensionLayer`）。`ComponentDocLayout` 传 `doc-tier`。

最低交付（`EXTENSION_MINIMUM_LAYERS`，**非累加**，以表为准）：

| tier | 必需层 |
|------|--------|
| `atom` | Props + Slots |
| `molecule` | Variants + Props + Slots |
| `organism` / `template` | Variants + Props + Slots + Composition |
| `scenes` | Variants + Props + **Scenarios**（不要求 Slots / Composition） |

Popup 外壳页不承载 Dialog/Detail/Verify 内容（§7）。

## 16. Crypto 双地址行（锁死）

**未经明确要求禁止改** `CryptoAddressSide.vue`、`CryptoCombo.module.css`（`.cryptoAddress*`）、`CryptoAddressTags.module.css` 的布局/DOM/ellipsis/Flotation 拆分。

不变量：`.eds-crypto-address-flotation` `flex: 0 1 auto`；trigger / `.eds-crypto-address-side` `width: max-content`；`.cryptoAddressLine` `flex-grow: 0`。地址截断用 `truncateAddressMiddle(value)`（`cryptoAddressUtils.ts`，默认 head 8 / tail 6；调用点**不传**长度参数），JS 截断后禁再加 CSS ellipsis。

## 17. Showcase 场景化组件命名（Eg* Scene）

业务场景须封装为 EDS 导出组件，Showcase 与 consumer **同一 `Eg*` 入口**，禁 preview 内手拼 primitives。

**组件名**：`Eg{Scene}{Base}` — Scene 为 PascalCase 场景语义，Base 为被封装族名（`Popover`、`Input`、`Button`、`Tag`、`Verify`、`Dialog`、`Flotation`…）。

**路由 slug**：新增一律 `{family}-scene-{scene}`（kebab-case）；`scene` 段与 `customize.scenario`、组件 Scene 语义一致。例：`gas-fee` ↔ `EgGasFeePopover`（侧栏可中文「矿工费」）；禁 `notes`/`miner-fee`/`guide` 等与组件名不符的键；禁 `scens` 拼写。

**已对齐**：

| 组件 | slug |
|------|------|
| `EgStatusTag` / `EgColorfulTag` / `EgBusinessTag` | `tag-scene-status` / `-colorful` / `-palette` |
| `EgTooltipOverflow` | `tooltip-scene-text-overflow`；同族 `tooltip-scene-paragraph-overflow`、`tooltip-scene-multi-address` |
| `EgSymbolDialog` / `EgBusinessDialog` | `dialog-scene-symbol` / `dialog-scene-compose` |
| `EgEmailVerify` 等六个 | `verify-scene-{email\|google\|login-password\|transaction-password\|passkey\|locked}` |
| `EgCregisNavBar` | `nav-bar-scene-cregis`（仅 cregis） |
| `EgCregisModuleMenu` / `EgUdunModuleMenu` | `module-menu-scene-cregis` / `-udun` |
| `EgModuleMenuTrigger` | `flotation-trigger-scene-module-menu` |
| `EgCascadeMenu` / `EgAddressDropdownMenu` / `EgAddressHoverMenu` | `flotation-box-scene-cascade-menu` / `-address-dropdown` / `-address-hover` |
| `EgDetailPopup` / `EgDialogPopup` / `EgVerifyPopup` | `popup-scene-detail` / `-dialog` / `-verify` |
| `EgGuidancePopover` / `EgRemarkPopover` / `EgGasFeePopover` / `EgConfirmPopover` | `popovers-scene-*` |
| `EgListFieldAddressLine` / `EgListFieldOverflowText` / `EgListFieldHashLikeLine` | Patterns `list-field-*`（文档 component-tag） |

**存量例外**（早于本约定，**不得**作为新增范本；改动时可顺带迁移，须同步 router legacy redirect）：

| 实际 slug | 组件 | 偏差 |
|-----------|------|------|
| `input-combo-input` / `input-search` / `input-verify-input` | `EgComboInput` / `EgSearchInput` / `EgVerifyInput` | 缺 `-scene-` 段 |
| `input-combo-textarea`（在 `textarea` 族下） | `EgComboTextarea` | 缺 `-scene-`，且前缀与 family 不符 |
| `button-combo` / `button-icon` / `button-icon-pro` / `button-link` / `button-pagination` | `EgComboButton`（含 `EgComboPopupButton`/`EgComboFloatButton`/`EgComboPageButton`）/ `EgIconButton` / `EgIconProButton` / `EgLinkButton` / `EgPaginationGroupButton` | 缺 `-scene-` 段 |
| `upload-file` / `upload-image` | 未实现 | 缺 `-scene-` 段 |
| `popovers-scene-*`（family slug 为 `popover`） | 见上 | family 单数、scene 前缀复数 |
| `filter-scene-standard` / `filter-scene-advanced` | 未实现（catalog 已注册，无预览与 `Eg*`） | slug 合规，缺实现 |

**对齐清单**（改场景时逐项核对）：

| 层 | 规则 |
|----|------|
| `catalog.ts` `children[].id` | = 路由 slug |
| 预览 `anchor-id` | `popovers-scene-{scenario}` 等 |
| `component-tag` / import snippet | 同一 `Eg*` |
| `splitScenePreviewEntries` | slug + `initialScenario` 一致 |
| 预览 Vue | 直接渲染目标 `Eg*`，非手搓组合 |

**内部实现**：panel / helper 可保留 domain 文件夹名（如 `miner-fee/`）；公共 export 仍用 `EgGasFeePopover`。旧 slug 仅 `router` legacy redirect。

**Verify 族结构**：单一 `verify` 族（本体 `EgVerify` 可切 type）+ `verify-scene-*` 六个场景页；**禁止**把 Email / Google 等拆成并列 family。

## 18. Showcase 定制面板状态

`ComponentDocLayout` 的 `customize-state` 一律用 `createDocCustomizeState<typeof xxxDefaults>(defaults, 场景覆盖)`（`componentDoc/customizeState.ts`）。

- 默认值字面量自动放宽为基础类型；控件运行期动态键（`boxItem3SymbolIcon` 等）由索引签名承载。
- **禁止** `reactive({ ...defaults })` 直接建状态（会收窄成只读字面量并丢失动态键）。
- 需要窄类型的地方在**消费点**转换（`customize.placement as TooltipPlacement`），不在状态定义处。

## 19. 变更记录

- 2026-09-09：§0.1 Agent 工作推进（大阶段 · 强逻辑）—— 过程可见、大阶段粒度、验证区分本次/既有；consumer 规则须对齐。
- 2026-09-09：规则自查修正 —— 删除从未使用的「维度命名 preview key」节，改为 §2 公共 API 与废弃；新增 §9 验证与交付；修正 `.eds-shadow-*-stack`（不存在）、`overlay-microfloat-flash-fix.md`（不存在）、`overflowMeasureDuringLayout.ts` 路径、`truncateAddressMiddle` 参数（默认 8/6）、`EgListFieldAddress`→`EgListFieldAddressLine`、§15 scenes tier 非累加与 `template` 缺失；§17 补存量 slug 例外表；变更记录移至末位并改号 §19。
- 2026-09-09：清空 `packages/components` 全部 `@deprecated` 公共 API（`LegacyButtonVariant`/`ButtonVariantInput`、`BatchBar.actionLabel`、`ModuleMenuItemLevel` 与 `ModuleMenuItem.level|active`、`AnchoredTooltip.microFloat`、`CryptoSymbol.showEntryBadge`）；consumer `work-cregis-desktop` 同步迁移旧原子名（`EgTooltip` 现指 AnchoredTooltip，面板改称 `EgTooltipPanel`）+ DS→EDS 术语清洗。
- 2026-09-09：§17 场景 slug 统一 `{family}-scene-{scene}` — `tag-scene-*`、`dialog-scene-*`、`flotation-box-scene-*`、`verify-scene-*`（Verify 合并为单一族）；router 补 legacy redirect。新增 §18 定制面板状态约定；Showcase typecheck 归零。
- 2026-09-09：§17 扩展示例表；原子/场景组件重命名（EgSearchInput、EgComboInput、EgLinkButton 等）+ 场景 component-tag 对齐。
- 2026-09-09：§17 场景化 `Eg*` 命名与 slug 对齐；Popovers `popovers-scene-*` + `EgGuidancePopover`/`EgGasFeePopover`；规则文件更名 `work.mdc`。
- 2026-09-01：§7 增补 `EgDataList` 批选布局动效 — EDS glue 边界、recipe 复用、禁止 semantic 场景类；§1 Motion 补 DataList 示例。
- 2026-08-25：Showcase 全 desktop 自包含 — 移除 `@eds/website-*` 依赖；与 eds-website 零关联。
- 2026-08-24：规则整合为单文件；全文精简浓缩。
- 更早见 git history。
