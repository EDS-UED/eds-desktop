---
description: EDS Desktop 工作约定（唯一真源）
alwaysApply: true
---

# EDS Desktop — 工作约定（唯一真源）

> **§0** 执行 · **§1** token · **§2** API · **§3–4** Showcase · **§5** HMR · **§6** 滚动 · **§7** Organism · **§8** 同步 · **§9** 验证 · **§10–18** 专项 · **§19** 推送 · **§20** 变更
>
> 外部引用固定：consumer `work-cregis-desktop` 指向 **§7**（EgPopup）与 **§13**（顶底毛玻璃）。**改此两节编号须同步 consumer `AGENTS.md` 与 `.cursor/rules/work.mdc`。**

## 0. Agent 执行范围

- 只做用户明确要求；**最小 diff**；禁顺带改 layout、API、consumer、文档、测试。
- 不确定先问；无 token / 无 prop 枚举 → **停改**，先问能否硬编码。
- `eds-mobile` 与 consumer `work-cregis-desktop` 的 `.cursor/rules/work.mdc` 须与 §0.1 对齐。

| 用户说 | ✅ | ❌ |
|--------|----|----|
| 表头溢出也用 `…` | 表头 `text-overflow: ellipsis` | 改 flex、tooltip、combo |
| 同步 | §8 全流程 | 改 consumer 业务、自动 commit |
| 推送 | §19 全流程 | 只 commit 不 push、漏文件、问「要不要 push」 |
| 生成数据 | §4 | 写 `*CustomizeDefaults` / localStorage |

### 0.1 协作方式

结对编程：**过程中**每个动作（读/搜/改/验）带一两句因果与依据 §；**禁止**闷头改完再五段式汇报。**收尾**两三句（做了什么、验证、还需什么）。心里顺序：目标 → EDS 真源 → sync → §9（**区分本次引入 vs 预先存在**）；改公共 API 须交代 consumer 影响。

## 1. Token 与样式

**作用域**：Showcase + Desktop 组件 → `@eds/desktop-tokens` / `@eds/desktop-components`；预览 subtree 包 `.desktopTokens`。

1. **语义优先**：`packages/tokens/spec/**`、`dist/css/**`；禁 `--scale-*` 顶替 `--radius-*` / `--spacing-*` / `--stroke-*`。
2. **禁私造 `--*`**、局部 alias、手拼 shadow/玻璃。阴影 `--eds-shadow-{deep|light|molde|subtle}`；玻璃 `.effect-*`（`container-box`、`flotation-box`、`popover-box`、`popup-box`、`molde-level`、`subtle-card`）。
3. **Motion**（base → recipe → semantic → glue）：
   - **base**：`--eds-motion-*` only；**recipe**：`--motion-recipe-*` 仅 glue；**semantic**：`.motion-page`、`.motion-flotation`、`.motion-layout`、`.motion-layout-deform`、`.motion-layout-content`、`.motion-deform`、`.motion-ease`、`.motion-none`（utilities）— 完整场景默认；**禁** semantic 写 `enter-from`/`leave-to`/方向 modifier。
   - **glue**：Vue lifecycle、叠层、方向；例 DataList 批选列宽、Motion Page Push/Pop（forward content +offset / backward opacity-only）、Layout Deform / Detail tab（`useMotionLayoutContentSwitch`）。**禁**把 glue 写入 `spec/motion/semantic.json`。详案：`layout-deform-page-switch.md`。
4. **microFloat**：opacity/transform 与 backdrop-filter **同在 glass**（`.effect-flotation-box` / `.effect-popup-box`）；glue `overlayGlassMicroFloat.module.css`；默认启用，无开关 prop。
5. **EgPopover（锁死）**：默认宽 336；预置 256/296/336/460。只改 shadow/颜色时 **禁**动 layout；`.effect-popover-box`；阴影仅 SVG `.outline`。禁为修闪烁去掉 opacity 或 hover `v-if` 销毁 Teleport。
6. **Typography**：`.typography-*` 或 `--eds-*-size/weight/line-height`。
7. **其它**：顶边距 `height: var(--padding-top-{xs|sm|md|lg})`；数字 `formatGroupedNumber()` + `tabular-nums`；Bar 11px → `.eds-bar-text-subpixel` 或 NavBar sizer（§7）。

| 说法 | 用 |
|------|-----|
| 圆角/间距/1px 边 12/12/1 | `--radius-md` / `--spacing-3` / `--stroke-sm` |
| 13px 常规/中黑 | `--eds-body-small-*` / `--eds-body-small-strong-*` |

顶底毛玻璃 → **§13**；优先 `EgSkid` / `EgToolBar` / `Paginer`。

## 2. 公共 API 与废弃

- **不留 `@deprecated`**：废弃当轮删净（prop、类型、export、兼容分支）。
- **破坏性变更同轮迁移 consumer** + §8 + §9；EDS build 绿 ≠ consumer 可用。
- **改名让位再占位**（例 `EgTooltip`↔`EgTooltipPanel`）：旧面板名→新面板名 在前，旧浮层→`EgTooltip` 在后，防 `Duplicate identifier`。
- 全仓改名扫描带 `--hidden`（含 `.cursor/rules/**`）。

## 3. Showcase 目录

顶栏 **Tokens · Animations · Components · Patterns · Workflows**（§14）；`/patterns` ≠ 侧栏 Scenes。

- `catalog.ts` `children[].id` = 路由 slug = `anchor-id` 后缀；**先 catalog + slug，再 section**。
- 同族一条 import；第二 child 起 `previewGroupLabel`。
- 场景化新增/对齐 → **§17**；新 Scenes slug `{family}-scene-{scene}`（kebab），禁 `scens`。

## 4. 文案与演示数据

章节中文；prop 标签「中文 + 英文 token」（`showcasePropLabels.ts`）。**生成数据**仅临时 `*DemoCustomizeState` 或会话 `Object.assign`；禁写 defaults / localStorage；`:customize-defaults` 始终永久 defaults。

## 5. 库作用域 · HMR

| 层 | 用 |
|----|-----|
| Showcase 全站 | `@eds/desktop-tokens` / `@eds/desktop-components` |
| 组件预览 subtree | `.desktopTokens` |

**禁** `eds-website` / `@eds/website-*`；`desktopTokens` 打 Eg* 根；Desktop CSS 用非 desktop token。

**HMR**：`packages/components/**` alias+HMR；`tokens/spec/**` watch rebuild。未生效先查 HMR/dist/specificity。

**Native reset**（`global.css`）：禁 `font: inherit`；禁 `button:not(.eds-xxx)` 抬特异性；typography `.class:global(.eds-*)`；`textarea` reset 排除 `.eds-textarea-control`。

## 6. 嵌套滚动边界

NavBar、ModuleMenu、FlotationMenu、DataList、Dialog、Detail、Skid 等：顶/底 **不**链式滚外层；`overflow-y: auto` + `overscroll-behavior: none`；JS `wheel`（passive:false）+ scroll 钳制 + **`SCROLL_EDGE_EPSILON = 2`**（各组件本地一致）。参考 `NavBar.vue`；顶底毛玻璃见 **§13**。

## 7. Organism · Showcase 场景化

`ComponentDocLayout` + 场景化下拉；`customize.scenario` 不进 snippet（`omitKeys`）。

### EgPopup（外壳 vs 内容）

外壳：遮罩、Esc、进出场、`uses` 尺寸；**不做**内容。内容：`EgDetail`/`EgDialog`/`EgVerify` 各页配置。Popup host 挂**客户端 shell**；遮罩盖 NavBar+侧栏+主区。

| slug 类型 | 定制 |
|-----------|------|
| `popup` | uses、dialog 类型、开关 |
| `popup-scene-{detail\|dialog\|verify}` | 外壳 × 内容 |
| `detail` / `dialog-*` / `verify` + `*-scene-*` | 内容全文案 |
| `popovers-popover` | 方向矩阵 |
| `popovers-scene-*` | 各 `Eg*Popover`（§17） |

`EgDialog` Symbol/Compose/Standard；`EgVerify` 尺寸随 type 固定。

### Popovers / Layout Deform / Flotation / Detail / DataList

- **Popovers**：视觉在 `Eg*Popover`；禁 Showcase 手搓 host CSS。备注/确认 `teleport-to="body"`；placement 变须 `updatePosition()`。矿工费 `data-miner-fee-popover` | `data-miner-fee-ton-like` / `data-miner-fee-tron`。旧 `popovers-scens-*` 仅 router 重定向。
- **Layout Deform**：禁 `.motion-page` / `mode="out-in"` 替代；`useMotionLayoutDeformPageSwitch` + `EgMotionLayoutContent`；shell morph + content `is-exiting`→120ms swap→`is-entering`；layout 绑 **`activePage`**。详案 `layout-deform-page-switch.md`。
- **Flotation**：`EgFlotation` + `flotationDocCustomize.ts`；Box 场景键独立；`EgModuleMenuItem` `.motion-ease.is-enter-only`。
- **EgDetail Apply_Item**（`detail-apply-item.md`）：必 `createDetailApplyItemRow`；多地址 `addressLayout: 'multi-collapsed'`；`toolbarDividerPinned` 控分割线。
- **EgDataList 批选动效**（internal）：公共 API 仅 `EgDataList`；禁 export 动效组件/recipe 进 semantic。真源 `data-list/`；`visibleSlotIndices = idleSlotIndices`；`<col class="layoutColumn">` + snapshot；300/320ms 三处同步。禁 rAF 写列宽、动画中 `v-if` 切列、`:key` remount。consumer §7.6。
- **其它**：NavBar preset `presets/<domain>/`；选中 `.motion-none`；Module Menu Cregis/UDun 独立 preset。

## 8. 本地同步

| 用户说 | 行为 |
|--------|------|
| 同步 | build eds-desktop → **所有** link consumer `pnpm install` |
| 同步到 X | 指定项目 |
| 列出可同步项目 | `pnpm sync:list`，不 build |

**单向** EDS→consumer ✅；consumer 回写 `packages/**` ❌（正向 API 迁移除外）。步骤：build/sync → consumer install → **§9** → 汇报。扫描 `~/Projects/**/package.json` 含 `link:`/`file:` 指向本仓 packages。`work-cregis-desktop` 100% Desktop；勿只 build 不同步。

## 9. 验证与交付

未过对应行 = **未完成**。

| 改动面 | 必跑 |
|--------|------|
| `packages/components/**` | `pnpm build:components` |
| `packages/tokens/**` | `pnpm build:tokens` |
| `apps/showcase/**` | `pnpm --filter @eds/desktop-showcase build` |
| 公共 export 增删改名 | 上列 + `pnpm sync` + consumer `vue-tsc` + `build` |
| consumer 改动 | `node scripts/verify-*.mjs` 全绿 |

批量改名回扫（`--hidden`）；词边界防子串误命中；失败区分本次 vs 预先存在（`git show HEAD:<file>` 复跑）。

## 10. Display P3

hex 仅 fallback；canonical `color(display-p3 …)`；消费 `var(--*)`；mix `color-mix(in display-p3, …)`；禁组件 sRGB hex 覆盖 token。

## 11. Corner Smoothing

squircle **60%**（`--corner-smoothing: 0.6`）；`var(--radius-*)` 禁硬编码 px；禁 per-component clip-path。例外：`.effect-{container|flotation|popover|popup}-box`、`data-no-corner-smoothing`。动态 UI：`rescanCornerSmoothing(root)`。

## 12. SVG 描边（EgIcon）

禁 `non-scaling-stroke`；`stroke-width: calc(var(--eds-icon-stroke-screen) * 32 / var(--eds-icon-display-px))`（`Icon.module.css`）。

## 13. Top & Bottom Mask

固定高面板内滚动：顶/底 sticky + `var(--effect-mask)` + `var(--eds-blur-bg)`；单滚动容器；禁 fixed 顶底栏/不透明 scrim。`SCROLL_EDGE_EPSILON = 2`（与 §6 一致）。参考 `Skid.vue`、`frostedPageChrome.css`。

## 14. EverGreen IA

一级 → `/tokens` … `/workflows`。Components：Atoms/Molecules/Organisms/Templates + Scenes。**Variant ≠ Scene**。Flotation 用 `navSubgroup`。catalog：`catalogHelpers.ts`、`navSection`、`buildCatalogAnchorItems`。

## 15. 组件文档扩展层

Variants / Props / Slots / Composition / Scenarios（`extensionDelivery.ts`）。最低交付（**非累加**）：

| tier | 必需 |
|------|------|
| atom | Props + Slots |
| molecule | Variants + Props + Slots |
| organism / template | + Composition |
| scenes | Variants + Props + Scenarios |

Popup 外壳不承载 Dialog/Detail/Verify（§7）。

## 16. Crypto 双地址行（锁死）

**未经明确要求禁止改** `CryptoAddressSide.vue`、`CryptoCombo.module.css`、`CryptoAddressTags.module.css` 布局/DOM/ellipsis/Flotation。

不变量：`.eds-crypto-address-flotation` `flex: 0 1 auto`；trigger/`side` `width: max-content`；`.cryptoAddressLine` `flex-grow: 0`。截断 `truncateAddressMiddle(value)`（默认 8/6，**不传**长度）；JS 截断后禁 CSS ellipsis。

## 17. Showcase 场景化（Eg* Scene）

业务场景封装 EDS `Eg*`，Showcase 与 consumer 同一入口；禁 preview 手拼 primitives。

- **命名** `Eg{Scene}{Base}`；**slug** `{family}-scene-{scene}`，与 `customize.scenario` 一致；禁 `scens` / 与组件不符的 scene 键。
- **对齐清单**：`catalog children[].id` = 路由 slug = `anchor-id`；`component-tag`/snippet 同一 `Eg*`；`splitScenePreviewEntries` 一致；预览直接渲染 `Eg*`。
- **已对齐示例**：`tag-scene-*`、`dialog-scene-*`、`verify-scene-*`、`popovers-scene-*`（`EgGuidancePopover`/`EgRemarkPopover`/`EgGasFeePopover`/`EgConfirmPopover`）、`module-menu-scene-{cregis|udun}`、`flotation-*-scene-*`、`popup-scene-*`、Patterns `list-field-*`。
- **存量例外**（禁作新增范本）：`input-combo-*`/`button-*`/`upload-*` 缺 `-scene-`；`popovers-scene-*` family 为单数 `popover`；`filter-scene-*` 未实现。改动可顺带迁移 + router legacy redirect。
- **Verify**：单一族 + `verify-scene-*` 六页；禁拆并列 family。内部 panel 文件夹名可 domain 化；公共 export 用 `Eg*`。

## 18. Showcase 定制面板状态

`createDocCustomizeState<typeof xxxDefaults>(defaults, 场景覆盖)`（`customizeState.ts`）。禁 `reactive({ ...defaults })`；窄类型在消费点 cast。

## 19. 推送（GitHub + Pages）

用户说 **「推送」** = **全部上 GitHub + 触发 Pages，本地/worktree 一点不剩**（除非当场另指定分支/仓）。

1. **§9** 全绿（区分本次 vs 预先存在）
2. **`git status`** — 已改/未跟踪全部纳入或用户明确排除；禁留 unstaged / 未 push commit
3. **commit** — 相关 untracked 一并 add；默认 `main`
4. **`git push`**（首 push `-u origin <branch>`）；禁只 commit 不 push
5. **Pages** — `main` 触发 `deploy-pages.yml` → `https://eds-ued.github.io/eds-desktop/`；确认 Actions 或告知用户

**禁**：问「要不要 push」、漏文件、推前跳过 §9、force push `main`（除非明确要求）。

## 20. 变更记录

- 2026-09-18：§19 推送；§1 Motion 补 `.motion-layout-content` / Motion Page content 方向分支；规则全文压缩合并。
- 2026-09-09：§0.1 协作方式；§2 API 废弃；§9 验证；§17 场景 slug/`Eg*` 对齐；§18 定制状态；清空 `@deprecated`；consumer DS→EDS 迁移。
- 2026-09-01：§7 EgDataList 批选动效 glue 边界。
- 2026-08-25：Showcase 自包含，移除 `@eds/website-*`。
- 2026-08-24：规则整合单文件。更早见 git history。
